// biome.json já criado pelo comando biome init
// Agora, criar configuração básica para ultracite

module.exports = {
	extends: ["ultracite"],
	rules: {
		// Adicione regras customizadas aqui, se necessário
	},
};
